#!/bin/bash
# Скрипт быстрого запуска интеграционных тестов

set -e

echo "🚀 Запуск интеграционных тестов TaskMind"

# Переходим в корневую директорию проекта
cd "$(dirname "$0")/.."

# Активируем виртуальную среду
if [ -f ".venv/bin/activate" ]; then
    source .venv/bin/activate
    echo "✅ Виртуальная среда активирована"
else
    echo "❌ Виртуальная среда не найдена. Запустите: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt"
    exit 1
fi

# Запускаем интеграционные тесты
echo "🧪 Запуск тестов..."
python integration_tests/run_integration_tests.py

echo "✅ Интеграционные тесты завершены"