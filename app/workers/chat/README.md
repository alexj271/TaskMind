# Chat Worker - Интеллектуальный агент для управления задачами

## Обзор

Chat Worker - это интеллектуальный агент для TaskMind, который позволяет пользователям управлять задачами через естественный диалог. Агент использует LLM (Large Language Model) с function calling для понимания намерений пользователя и выполнения действий с задачами.

## Архитектура

### Основные компоненты

1. **TaskTools** (`app/workers/chat/tasks.py`) - Набор инструментов для работы с задачами
   - `create_task()` - Создание новых задач
   - `search_tasks()` - Семантический поиск задач с использованием embeddings
   - `update_task()` - Обновление существующих задач
   - `get_user_tasks()` - Получение списка задач пользователя

2. **DialogMemoryService** (`app/workers/chat/memory_service.py`) - Управление памятью диалога
   - Отслеживание целей пользователя
   - История действий с задачами
   - Контекст диалога и уточнения

3. **Модели данных** (`app/workers/chat/models.py`)
   - `MemorySummary` - Резюме памяти диалога
   - `DialogGoal` - Цели диалога (создание, поиск, редактирование задач)
   - `TaskAction` - Действия с задачами (создано, обновлено, удалено)

4. **AI промпты** (`app/prompts/`)
   - `system_chat_agent.md` - Системный промпт для AI агента
   - `task_tools.md` - Описание доступных инструментов

## Возможности

### 1. Создание задач через естественный язык
```
Пользователь: "Нужно не забыть купить хлеб завтра"
Агент: "Создал задачу 'Купить хлеб' на завтра 📅 Хотите добавить напоминание?"
```

### 2. Семантический поиск задач
```
Пользователь: "Где моя задача про работу?"
Агент: [использует embeddings для поиска] "Нашёл: 📝 'Подготовить отчет' - создана 3 дня назад"
```

### 3. Обновление и завершение задач
```
Пользователь: "Отчет готов"
Агент: "Отлично! ✅ Отметил 'Подготовить отчет' как выполненную"
```

### 4. Контекстное общение
- Помнит цели пользователя в диалоге
- Учитывает историю действий
- Предлагает связанные действия

## Интеграция с TaskMind

### Используемые сервисы
- **OpenAI Service** - LLM с function calling
- **TaskRepository** - CRUD операции с задачами + семантический поиск
- **UserRepository** - Работа с пользователями
- **PromptManager** - Управление AI промптами
- **Telegram API** - Отправка ответов

### Подключение к Gatekeeper Worker
Chat Worker интегрируется с Gatekeeper Worker через классификацию сообщений:
- Gatekeeper определяет, является ли сообщение задачей или разговором
- Разговорные сообщения перенаправляются в Chat Worker
- Chat Worker может создавать задачи по запросу пользователя

## Технические особенности

### Function Calling
Агент использует OpenAI function calling для вызова методов TaskTools:
```python
available_functions = {
    "create_task": task_tools.create_task,
    "search_tasks": task_tools.search_tasks,
    "update_task": task_tools.update_task,
    "get_user_tasks": task_tools.get_user_tasks
}
```

### Семантический поиск
Использует существующую систему embeddings (BGE-small) для поиска релевантных задач по смыслу запроса пользователя.

### Память диалога
- Сохраняет контекст беседы в Redis/PostgreSQL
- Отслеживает цели пользователя
- Ведет историю действий с задачами
- Автоматически очищает устаревшую память

## Тестирование

Полное покрытие тестами:
- **Unit тесты** для TaskTools (создание, поиск, обновление задач)
- **Integration тесты** для основного процессора сообщений
- **Mocking** всех внешних зависимостей (OpenAI, DB, Telegram)
- **Error handling** тесты для обработки ошибок

Запуск тестов:
```bash
pytest app/workers/chat/tests/ -v
```

## Конфигурация

### Environment Variables
- `OPENAI_API_KEY` - API ключ для OpenAI
- `GPT_MODEL_FULL` - Модель GPT для Chat Worker (по умолчанию gpt-4)

### Prompts
- Промпты находятся в `app/prompts/`
- Поддерживают шаблонизацию с параметрами
- Легко редактируются для настройки поведения агента

## Примеры использования

### Создание задачи
```python
# Пользователь пишет: "Напомни позвонить маме"
# Агент вызывает:
await task_tools.create_task(
    title="Позвонить маме",
    description="Не забыть позвонить"
)
```

### Поиск задач
```python
# Пользователь пишет: "Найди задачи про работу"
# Агент вызывает:
result = await task_tools.search_tasks("работа", limit=5)
```

### Обновление статуса
```python
# Пользователь пишет: "Задача выполнена"
# Агент сначала ищет задачу, потом обновляет:
await task_tools.update_task(task_id="...", completed=True)
```

## Будущие улучшения

1. **Расширенная память** - Долгосрочная память о предпочтениях пользователя
2. **Планирование задач** - Автоматическое предложение оптимального времени
3. **Интеграция с календарем** - Синхронизация с внешними календарями
4. **Групповые задачи** - Поддержка совместной работы над задачами
5. **Аналитика** - Статистика по выполнению задач и продуктивности

## Заключение

Chat Worker представляет собой мощный инструмент для естественного взаимодействия пользователей с системой управления задачами. Он объединяет современные возможности LLM, семантический поиск и память диалога для создания интуитивного пользовательского опыта.