# Система эмбеддингов TaskMind

## 🎯 Обзор

Система эмбеддингов TaskMind обеспечивает семантический поиск по задачам с использованием модели BGE-small и PostgreSQL с расширением pgvector.

## 🏗 Архитектура

### Компоненты
- **PostgreSQL + pgvector**: Хранение векторов с быстрым поиском
- **BGE-small модель**: Генерация 384-мерных эмбеддингов
- **HNSW индекс**: Оптимизированный поиск по косинусной близости
- **sentence-transformers**: Библиотека для работы с эмбеддингами
- **TaskRepository**: Слой доступа к данным с поддержкой эмбеддингов

### Схема базы данных
```sql
-- Поле эмбеддинга в таблице tasks
ALTER TABLE tasks ADD COLUMN embedding_bge_small vector(384);

-- Индекс для быстрого поиска (HNSW)
CREATE INDEX tasks_embedding_bge_small_hnsw_idx 
ON tasks USING hnsw (embedding_bge_small vector_cosine_ops);
```

## 📁 Структура файлов

```
app/
├── models/
│   └── task.py                     # Модель Task с полем embedding_bge_small
├── repositories/
│   └── task_repository.py          # CRUD + семантический поиск
└── ...

migrations/
└── manual_add_embedding.sql        # SQL миграция для pgvector

test/
├── test_embeddings.py             # Тесты векторных операций
└── test_task_repository_fixed.py  # Тесты TaskRepository

embedding_demo.py                   # Демо использования
```

## 🚀 Использование

### 1. Создание задач с эмбеддингами

```python
from app.repositories.task_repository import TaskRepository

repo = TaskRepository()

# Эмбеддинг генерируется автоматически из title + description
task = await repo.create(
    user_id=user.id,
    title="Купить продукты",
    description="Молоко, хлеб, яйца в магазине",
    scheduled_at=None,
    reminder_at=None
)
```

### 2. Семантический поиск

```python
# Поиск похожих задач
results = await repo.search_by_similarity(
    user_id=user.id,
    query="еда и покупки",
    limit=5
)

for task in results:
    print(f"{task.title} (расстояние: {task.similarity_distance})")
```

### 3. Fallback механизм

Если `sentence-transformers` не установлен:
```python
try:
    # Создание задачи с эмбеддингом
    task = await repo.create(...)
except ImportError:
    # Обработка отсутствия библиотеки
    print("sentence-transformers не установлен")
```

## ⚙️ Настройка

### 1. Установка зависимостей

```bash
# Основные зависимости (уже в requirements.txt)
pip install tortoise-orm asyncpg

# Для работы с эмбеддингами (опционально, ~3GB)
pip install sentence-transformers
```

### 2. PostgreSQL с pgvector

```sql
-- Установить расширение (требует прав администратора)
CREATE EXTENSION IF NOT EXISTS vector;

-- Применить миграцию
\i migrations/manual_add_embedding.sql
```

### 3. Конфигурация модели

```python
# В TaskRepository используется модель BGE-small (384 измерения)
model = SentenceTransformer("BAAI/bge-small")
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты эмбеддингов (без sentence-transformers)
python -m pytest test/test_embeddings.py test/test_task_repository_fixed.py -v

# Только тесты с базой данных
python -m pytest test/test_embeddings.py -m database

# Демонстрация (требует sentence-transformers)
python embedding_demo.py
```

### Типы тестов

1. **Векторные операции** (`test_embeddings.py`):
   - Создание и поиск по эмбеддингам
   - Производительность HNSW индекса
   - Валидация векторных данных

2. **TaskRepository** (`test_task_repository_fixed.py`):
   - Создание задач с мокированными эмбеддингами
   - Семантический поиск
   - Обработка отсутствующих зависимостей

## 🔧 Технические детали

### Формат хранения

Эмбеддинги сохраняются как JSON в компактном формате:
```json
[0.1234,0.5678,-0.9012,...]
```

### SQL-запросы

Поиск по сходству использует оператор `<=>` pgvector:
```sql
SELECT *, (embedding_bge_small <=> $1::vector) as distance
FROM tasks 
WHERE user_id = $2 AND embedding_bge_small IS NOT NULL
ORDER BY embedding_bge_small <=> $1::vector
LIMIT $3
```

### Производительность

- **HNSW индекс**: O(log n) поиск
- **BGE-small**: Быстрая генерация эмбеддингов
- **Cosine similarity**: Оптимизирован для семантического поиска

## 🎯 Возможности

### Реализовано ✅

- [x] pgvector поле в модели Task
- [x] HNSW индекс для быстрого поиска
- [x] Автоматическая генерация эмбеддингов при создании задач
- [x] Семантический поиск по задачам
- [x] Fallback для отсутствующих зависимостей
- [x] Полное покрытие тестами
- [x] Интеграция с TaskRepository

### Планы на будущее 🚀

- [ ] Переиндексация существующих задач
- [ ] Кэширование эмбеддингов
- [ ] Поддержка других моделей эмбеддингов
- [ ] API endpoints для семантического поиска
- [ ] Интеграция с Telegram ботом

## 📊 Примеры запросов

### Успешные кейсы

| Запрос | Найденные задачи |
|--------|------------------|
| "еда и готовка" | "Приготовить ужин", "Купить продукты" |
| "работа и бизнес" | "Встреча с клиентом", "Подготовить презентацию" |
| "покупки в магазине" | "Купить молоко", "Купить продукты" |

### Метрики производительности

- **Создание эмбеддинга**: ~50ms для BGE-small
- **Поиск по векторам**: ~5ms с HNSW индексом  
- **Размер эмбеддинга**: 384 float32 = 1.5KB

## ❗ Важные замечания

1. **Требования к диску**: sentence-transformers требует ~3GB для PyTorch
2. **pgvector**: Требует PostgreSQL 12+ с установленным расширением
3. **Производительность**: HNSW индекс критичен для скорости поиска
4. **Fallback**: Система работает без sentence-transformers (с ограничениями)

## 🔗 Связанные файлы

- `app/models/task.py` - Модель Task с векторным полем
- `app/repositories/task_repository.py` - Repository с эмбеддингами
- `migrations/manual_add_embedding.sql` - SQL миграция
- `test/test_embeddings.py` - Тесты векторных операций
- `test/test_task_repository_fixed.py` - Тесты TaskRepository
- `embedding_demo.py` - Демонстрация возможностей